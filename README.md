# vscode-dotfiles

This repository contains dotfiles to be copied into a dev container in VS Code.
By default, all files in this repository will be copied to the `~/.dotfiles` directory in the dev container, as well as the `~` directory.
The dotfiles assume ZSH is installed.
