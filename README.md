# vscode-dotfiles

This repository contains scripts and dotfiles to be copied into a dev container in VS Code.

By default, all files in this repository will be copied to the `~/.dotfiles` directory in the dev container, then `install.sh` will be run.

The dotfiles assume ZSH is installed.
